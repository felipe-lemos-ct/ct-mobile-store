import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ct-mobile-store',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
