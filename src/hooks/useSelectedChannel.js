function useSelectedChannel() {
  const channel = '';
  const setChannel = (channel) =>
    //@todo: set a channel (store)
    88;
  return {
    channel,
    setChannel,
  };
}
export default useSelectedChannel;
