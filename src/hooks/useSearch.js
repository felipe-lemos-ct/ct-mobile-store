const useSearch = () => {
  //@todo: search value can come from route query
  //  for example: /someurl?q=shirt
  const setSearch = (q) => {
    //@todo: set the route query to the q value (search value)
  };
  const search = '';
  return {
    search,
    setSearch,
  };
};
export default useSearch;
